services:
  - type: web
    name: text-angel-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn text_angel_api:app --host 0.0.0.0 --port 10000
    plan: free
    branch: main
    autoDeploy: true
